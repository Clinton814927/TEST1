# TEST11. let and const are both used to declare variables.
Let is used to reassign a values in javascript.
while const cannot be used.

both const and lets are block-scoped, there only exist in block formate.
e.g let count =1;
const names ='john';


sematic html  elements like <header>,<section>,<article> and <footer> clearly define thier purposed
    , improving accessibility and Seo.
         Non-semantic elementss like <div> and<span> do not decribe the content
            sematic is easier for screen readers and search engines understanding page.


            
